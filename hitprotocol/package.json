{
  "name": "hitprotocol",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "openzeppelin-solidity": "^1.11.0",
    "truffle": "^4.1.13",
    "truffle-flattener": "^1.2.5"
  },
  "devDependencies": {},
  "scripts": {
    "test": "mocha",
    "clean": "rm -rf build && mkdir build",
    "compile": "truffle compile",
    "flatten": "truffle-flattener contracts/HITToken.sol > build/HITToken.sol",
    "build": "npm run clean && npm run compile && npm run flatten"
  },
  "author": "",
  "license": "ISC"
}
